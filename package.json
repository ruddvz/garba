{
  "name": "garba-music",
  "version": "0.4.0",
  "private": true,
  "description": "Immersive, responsive, installable Gujarati Garba music player.",
  "scripts": {
    "catalogue": "node scripts/build-catalogue.mjs",
    "serve": "python3 -m http.server 4173",
    "check": "npm run catalogue && node --check app.js && node --check catalogue-bootstrap.js && node --check playback-bridge.js && node --check sw.js && node scripts/validate.mjs"
  }
}
