name = "playgarba-pga-rollup"
main = "rollup-worker.js"
compatibility_date = "2026-09-09"

[vars]
EVENTS_DATASET_NAME = "playgarba_events_v1"
PRESENCE_DATASET_NAME = "playgarba_presence_v1"
CF_ACCOUNT_ID = "REPLACE_WITH_CLOUDFLARE_ACCOUNT_ID"

[[d1_databases]]
binding = "DB"
database_name = "playgarba-pga"
database_id = "REPLACE_WITH_D1_DATABASE_ID"

[triggers]
# 00:40 IST. The job repairs the seven most recent closed IST days, so this is
# intentionally after midnight rather than exactly at the day boundary.
crons = ["10 19 * * *"]

# Set ANALYTICS_API_TOKEN with `wrangler secret put ANALYTICS_API_TOKEN`.
# This Worker should have no public custom-domain route.
