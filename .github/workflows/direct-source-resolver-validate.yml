name: Direct source resolver validate

on:
  pull_request:
    paths:
      - 'src/playback/direct-source-resolver.js'
      - 'scripts/lib/test-direct-source-resolver.mjs'
      - '.github/workflows/direct-source-resolver-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/playback/direct-source-resolver.js'
      - 'scripts/lib/test-direct-source-resolver.mjs'
      - '.github/workflows/direct-source-resolver-validate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Check resolver syntax
        run: node --check src/playback/direct-source-resolver.js
      - name: Test direct-source resolver
        run: node scripts/lib/test-direct-source-resolver.mjs
