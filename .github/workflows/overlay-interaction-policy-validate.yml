name: Overlay interaction policy validate

on:
  pull_request:
    paths:
      - 'src/accessibility/overlay-interaction-policy.js'
      - 'scripts/lib/test-overlay-interaction-policy.mjs'
      - '.github/workflows/overlay-interaction-policy-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/accessibility/overlay-interaction-policy.js'
      - 'scripts/lib/test-overlay-interaction-policy.mjs'
      - '.github/workflows/overlay-interaction-policy-validate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Test overlay interaction policy
        run: node scripts/lib/test-overlay-interaction-policy.mjs
