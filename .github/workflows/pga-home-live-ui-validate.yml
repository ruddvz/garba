name: PGA Home + Live UI validate

on:
  pull_request:
    paths:
      - 'src/pga/app/index.html'
      - 'src/pga/app/home-live.js'
      - 'src/pga/app/home-live.css'
      - 'scripts/lib/validate-pga-home-live-ui.mjs'
      - '.github/workflows/pga-home-live-ui-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/pga/app/index.html'
      - 'src/pga/app/home-live.js'
      - 'src/pga/app/home-live.css'
      - 'scripts/lib/validate-pga-home-live-ui.mjs'
      - '.github/workflows/pga-home-live-ui-validate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Validate PGA Home + Live UI truth
        run: node scripts/lib/validate-pga-home-live-ui.mjs
