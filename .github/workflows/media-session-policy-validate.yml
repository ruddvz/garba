name: Media Session policy validate

on:
  pull_request:
    paths:
      - 'src/playback/media-session-policy.js'
      - 'scripts/lib/test-media-session-policy.mjs'
      - '.github/workflows/media-session-policy-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/playback/media-session-policy.js'
      - 'scripts/lib/test-media-session-policy.mjs'
      - '.github/workflows/media-session-policy-validate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Check policy syntax
        run: node --check src/playback/media-session-policy.js
      - name: Test Media Session capability policy
        run: node scripts/lib/test-media-session-policy.mjs
