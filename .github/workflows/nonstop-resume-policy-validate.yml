name: Nonstop resume policy validate

on:
  pull_request:
    paths:
      - 'src/playback/nonstop-resume-policy.js'
      - 'scripts/lib/test-nonstop-resume-policy.mjs'
      - '.github/workflows/nonstop-resume-policy-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/playback/nonstop-resume-policy.js'
      - 'scripts/lib/test-nonstop-resume-policy.mjs'
      - '.github/workflows/nonstop-resume-policy-validate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Test Nonstop resume policy
        run: node scripts/lib/test-nonstop-resume-policy.mjs
