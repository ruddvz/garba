name: Direct media authority state validate

on:
  pull_request:
    paths:
      - 'src/playback/direct-media-authority-state.js'
      - 'scripts/lib/test-direct-media-authority-state.mjs'
      - '.github/workflows/direct-media-authority-state-validate.yml'
  push:
    branches:
      - main
    paths:
      - 'src/playback/direct-media-authority-state.js'
      - 'scripts/lib/test-direct-media-authority-state.mjs'
      - '.github/workflows/direct-media-authority-state-validate.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Check authority reducer syntax
        run: node --check src/playback/direct-media-authority-state.js
      - name: Test direct-media authority state reducer
        run: node scripts/lib/test-direct-media-authority-state.mjs
